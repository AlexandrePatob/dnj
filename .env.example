# URL pública da API, incluindo o prefixo configurado no backend (API_PREFIX=/v1).
NEXT_PUBLIC_API_URL=/api/v2
# Swagger/develop V2 upstream; override only when targeting another environment.
DNJ_V2_UPSTREAM_URL=https://ttwkfudhvvhuhp5yvsoydxggum0ictpg.lambda-url.sa-east-1.on.aws/v2

# Use "true" enquanto a API ainda não estiver disponível no ambiente local.
NEXT_PUBLIC_USE_MOCKS=true

# Supabase compartilhado: configure apenas no ambiente local/Vercel.
# A chave secreta é exclusiva do servidor; nunca use o prefixo NEXT_PUBLIC_ nela.
SUPABASE_URL=https://kawuwdvrisvbkucsnbep.supabase.co
SUPABASE_SECRET_KEY=
