"use client";
import { ArrowLeft, MapPin } from "lucide-react";
import { MAP_PINS, SPACES } from "@/features/app/fixtures";
import type { AnimDir } from "@/features/app/types";
import { useState } from "react";
export function EventMapScreen({ animDir, onBack }: { animDir: AnimDir; onBack: () => void }) { const [selected, setSelected] = useState(0); const space = SPACES.find((item) => item.name === MAP_PINS[selected].label) ?? SPACES[0]; return <main className="absolute inset-0 overflow-y-auto px-5 pb-28" style={{ background: "var(--background)", paddingTop: "calc(64px + var(--safe-area-top))" }}><button type="button" onClick={onBack} aria-label="Voltar"><ArrowLeft /></button><h1 className="mt-4 text-2xl font-black">Mapa do evento</h1><p className="mt-1 text-sm" style={{ color: "var(--muted-foreground)" }}>Mapa demonstrativo, não atualizado ao vivo.</p><div className="mt-5 grid grid-cols-2 gap-3">{MAP_PINS.map((pin, index) => <button type="button" key={pin.id} onClick={() => setSelected(index)} className="rounded-2xl p-4 text-left" style={{ background: index === selected ? "var(--primary-alpha-10)" : "var(--card)", border: "1px solid var(--border)" }}><MapPin size={18} style={{ color: pin.color }} /><span className="mt-2 block text-sm font-bold">{pin.label}</span></button>)}</div><section className="mt-5 rounded-2xl p-4" style={{ background: "var(--card)", border: "1px solid var(--border)" }}><h2 className="font-black">{space.name}</h2><p className="mt-2 text-sm" style={{ color: "var(--muted-foreground)" }}>{space.desc}</p></section></main>; }
