"use client";
import { ArrowLeft } from "lucide-react";
import type { AnimDir } from "@/features/app/types";
const items = [{ period: "Agora", time: "14:00", title: "Acolhida e missão de abertura", place: "Espaço Juventude" }, { period: "Em seguida", time: "15:00", title: "Celebração jovem", place: "Palco central" }, { period: "Mais tarde", time: "16:30", title: "Partilhas nos espaços", place: "Espaço Esperança" }];
export function EventScheduleScreen({ animDir, onBack }: { animDir: AnimDir; onBack: () => void }) { return <main className="absolute inset-0 overflow-y-auto px-5 pb-28" style={{ background: "var(--background)", paddingTop: "calc(64px + var(--safe-area-top))" }}><button type="button" onClick={onBack} aria-label="Voltar"><ArrowLeft /></button><h1 className="mt-4 text-2xl font-black">Cronograma</h1><p className="mt-1 text-sm" style={{ color: "var(--muted-foreground)" }}>Programação demonstrativa, não atualizada ao vivo.</p>{items.map((item) => <section key={item.period} className="mt-5 rounded-2xl p-4" style={{ background: "var(--card)", border: "1px solid var(--border)" }}><p className="text-xs font-bold" style={{ color: "var(--primary)" }}>{item.period}</p><h2 className="mt-2 font-black">{item.title}</h2><p className="mt-1 text-sm">{item.time} · {item.place}</p></section>)}</main>; }
